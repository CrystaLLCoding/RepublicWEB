# Republic Barbershop - Backend Setup

Полноценный backend на Node.js + Express + SQLite для сайта барбершопа.

## 📋 Требования

- Node.js (версия 14 или выше)
- npm (обычно идет с Node.js)

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты:
- `express` - веб-сервер
- `sqlite3` - база данных
- `multer` - загрузка файлов
- `bcryptjs` - хеширование паролей
- `jsonwebtoken` - JWT токены для авторизации
- `cors` - для CORS запросов

### 2. Запуск сервера

**Обычный запуск:**
```bash
npm start
```

**Запуск с автоперезагрузкой (для разработки):**
```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

### 3. Доступ к сайту

- **Главный сайт:** http://localhost:3000
- **Админ панель:** http://localhost:3000/admin

### 4. Данные для входа в админ панель

- **Логин:** `admin`
- **Пароль:** `admin123`

⚠️ **Важно:** После первого запуска измените пароль администратора!

## 📁 Структура проекта

```
republic/
├── server.js          # Основной серверный файл
├── package.json       # Зависимости проекта
├── database.sqlite    # База данных (создается автоматически)
├── uploads/           # Папка для загруженных изображений
├── index.html         # Главная страница сайта
├── admin.html         # Админ панель
├── styles.css         # Стили главного сайта
├── admin.css          # Стили админ панели
├── script.js          # JavaScript главного сайта
└── admin.js           # JavaScript админ панели
```

## 🔌 API Endpoints

### Авторизация
- `POST /api/auth/login` - Вход в систему

### Услуги
- `GET /api/services` - Получить все услуги
- `POST /api/services` - Добавить услугу (требует авторизации)
- `PUT /api/services/:id` - Обновить услугу (требует авторизации)
- `DELETE /api/services/:id` - Удалить услугу (требует авторизации)

### Мастера
- `GET /api/masters` - Получить всех мастеров
- `POST /api/masters` - Добавить мастера (требует авторизации)
- `PUT /api/masters/:id` - Обновить мастера (требует авторизации)
- `DELETE /api/masters/:id` - Удалить мастера (требует авторизации)

### Галерея
- `GET /api/gallery` - Получить все изображения
- `POST /api/gallery` - Загрузить изображение (требует авторизации, multipart/form-data)
- `DELETE /api/gallery/:id` - Удалить изображение (требует авторизации)

### Отзывы
- `GET /api/reviews` - Получить все отзывы
- `POST /api/reviews` - Добавить отзыв (требует авторизации)
- `DELETE /api/reviews/:id` - Удалить отзыв (требует авторизации)

### Настройки
- `GET /api/settings` - Получить все настройки
- `PUT /api/settings` - Обновить настройки (требует авторизации)

## 🗄️ База данных

База данных SQLite создается автоматически при первом запуске.

**Таблицы:**
- `services` - Услуги
- `masters` - Мастера
- `gallery` - Галерея изображений
- `reviews` - Отзывы клиентов
- `settings` - Настройки сайта
- `admin_users` - Пользователи админ панели

## 🔒 Безопасность

1. **JWT токены** - все запросы к защищенным endpoints требуют токен авторизации
2. **Хеширование паролей** - пароли хранятся в захешированном виде (bcrypt)
3. **Валидация файлов** - загружаются только изображения до 5MB

## 📝 Изменение пароля администратора

Для изменения пароля можно использовать SQL запрос или создать отдельный скрипт:

```javascript
const bcrypt = require('bcryptjs');
const sqlite3 = require('sqlite3').verbose();
const db = new sqlite3.Database('database.sqlite');

const newPassword = 'ваш_новый_пароль';
const hashedPassword = bcrypt.hashSync(newPassword, 10);

db.run('UPDATE admin_users SET password = ? WHERE username = ?', [hashedPassword, 'admin'], (err) => {
    if (err) {
        console.error(err);
    } else {
        console.log('Пароль изменен!');
    }
    db.close();
});
```

## 🌐 Деплой на сервер

### Вариант 1: VPS сервер

1. Загрузите файлы на сервер
2. Установите Node.js
3. Выполните `npm install`
4. Запустите `npm start`
5. Используйте PM2 для постоянной работы: `pm2 start server.js`

### Вариант 2: Хостинг с поддержкой Node.js

Многие хостинги (Heroku, Railway, Render, Vercel) поддерживают Node.js. Обычно нужно:
1. Загрузить код
2. Указать команду запуска: `npm start`
3. Указать порт из переменной окружения `PORT`

## ⚙️ Переменные окружения (опционально)

Создайте файл `.env` для настройки:

```
PORT=3000
JWT_SECRET=your-secret-key-here
```

Затем установите пакет `dotenv` и загрузите переменные в `server.js`:

```javascript
require('dotenv').config();
const PORT = process.env.PORT || 3000;
const JWT_SECRET = process.env.JWT_SECRET || 'default-secret';
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Установлены ли все зависимости (`npm install`)
2. Не занят ли порт 3000 другим приложением
3. Права на запись в папку `uploads/` и создание `database.sqlite`

## 🎉 Готово!

Теперь у вас полноценный backend с базой данных! Все данные сохраняются на сервере и доступны всем пользователям сайта.


